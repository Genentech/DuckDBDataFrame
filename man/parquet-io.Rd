% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet-io.R
\name{parquet-io}
\alias{parquet-io}
\alias{validateAppendOffset}
\alias{readParquetSchema}
\alias{reconcileParquetSchema}
\alias{checkHiveAppendPartitions}
\alias{checkAppendPart}
\title{Parquet append validation for the DuckDB package suite}
\usage{
validateAppendOffset(offset)

readParquetSchema(path, columns = NULL)

reconcileParquetSchema(path, columns, arrow_types)

checkHiveAppendPartitions(
  path,
  indexcols,
  grid_suffix,
  grid,
  along,
  group_offset
)

checkAppendPart(path, part, part_digits = 0L)
}
\arguments{
\item{offset}{Non-negative integer scalar; global index shift for flat
table append (\code{\link{validateAppendOffset}}).}

\item{path}{Directory containing Parquet files or hive partitions.}

\item{columns}{Character vector of column names required in an existing
dataset schema.}

\item{arrow_types}{Named list of \code{\link[arrow]{DataType}} objects
(names must match \code{columns}). \code{NULL} elements adopt the on-disk
type; non-\code{NULL} elements must match the existing type exactly
(\code{\link{reconcileParquetSchema}}).}

\item{indexcols}{Character vector of index column names for hive layout.}

\item{grid_suffix}{Partition directory suffix (e.g. \code{"group__"}).}

\item{grid}{\link[S4Arrays]{ArrayGrid} for the current write slab.}

\item{along}{Integer index of the append dimension along which
\code{group_offset} applies.}

\item{group_offset}{Partition-group offset along \code{along} for hive
append.}

\item{part}{Non-negative integer; flat \code{part-<n>.parquet} index.}

\item{part_digits}{Zero-padding width for \code{part} in the filename.}
}
\value{
\describe{
  \item{\code{validateAppendOffset}}{Integer scalar.}
  \item{\code{readParquetSchema}}{\code{\link[arrow]{Schema}} object.}
  \item{\code{reconcileParquetSchema}}{Named list of resolved
    \code{DataType} objects.}
  \item{\code{checkHiveAppendPartitions}, \code{checkAppendPart}}{
    \code{NULL}, invisibly.}
}
}
\description{
Low-level guards for incremental Parquet writes used by
\pkg{BiocDuckDB} and \pkg{DuckDBArray}. End users should call
\code{\link[BiocDuckDB]{writeParquet}} or
\code{\link[DuckDBArray]{writeCoordArray}} rather than these functions
directly.
}
\details{
These functions implement the shared append contract (fail before write,
no silent overwrite):
\itemize{
  \item Flat sample/feature tables use \code{part-*.parquet} files;
    \code{checkAppendPart} and \code{reconcileParquetSchema} apply.
  \item Hive-partitioned coord arrays use
    \code{<index><suffix>=<n>/} directories;
    \code{checkHiveAppendPartitions} guards partition collisions.
  \item \code{readParquetSchema} reads the first \code{*.parquet} file
    found under \code{path} (recursive search).
}
}
\seealso{
\code{\link[BiocDuckDB]{writeParquet}},
\code{\link[DuckDBArray]{writeCoordArray}}
}
\author{
Patrick Aboyoun
}
\keyword{internal}
